export enum Role {
    User = "user",
    Admin = "admin",
  }
  
  export enum Status {
    Approved = "Approved",
    Declined = "Declined",
    Pending = "Pending",
  }

  export enum SensorType {
    ReefNet = "ReefNet",
    CTDSensor = "CTDSensor",
    PluvioMeter = "PluvioMeter",
  }